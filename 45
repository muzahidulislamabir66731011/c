
#include <stdio.h>

unsigned invert(unsigned x, int p, int n)
{
    unsigned mask, left, right, field;

    mask = ~(~0u << n);

    mask = mask << (p + 1 - n);

    return x ^ mask;
}

int main(void)
{
    unsigned x = 0b10101010;
    int p = 5;
    int n = 3;

    printf("Before: %u\n", x);
    printf("After : %u\n", invert(x, p, n));

    return 0;
}
