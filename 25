#include <stdio.h>
#define MAXLINE 10
int main(void) {
    char line[MAXLINE] = ("abirabirk");
    char xine[MAXLINE] = {0};
    for (int i = 0, j = MAXLINE - 2; i < 10; i++ , j--)
    {
        xine[i] = line[j];
    }
    xine[MAXLINE - 1] = '\0';
    
    puts(xine);
    return 0;
}
